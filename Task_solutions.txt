1. Task: Changing time zones:
$ date
$ timedatectl
$ sudo timedatectl set-timezone <time_zone_you_want>
Example:
$ sudo timedatectl set-timezone Africa/Banjul

2. Task: File permission in a remote server:
$ ssh banner@172.16.238.12 then password

$ sudo su
$ chmod +rx xfusion.sh

3. Task creating a user with a non-interactive shell
$ ssh tony@<ip_address>
passwrod
$ sudo su
Checked first id james
$ id james
id: james: no such user
$ add user james -s /sbin/nologin
Check:
id james
$ cat /etc/passwd | grep james

4. NTP_setup task:
Ususal login then 
sudo su
sudo apt-get update
rpm -qa | grep ntp
ntpq
ntpq> quit
ntpstat, ntpstat
systemctl status ntp
systemctl status ntpd
systemctl start ntpd
systemctl status ntpd

7th May 2022:
5. Install a package:
sudo yum install squid
check this link:
https://www.2daygeek.com/enable-disable-services-on-boot-linux-chkconfig-systemctl-command/
systemctl status squid
systemctl enable squid
systemctl start squid

6. Task Mariadb troubleshooting:
Regular ssh to jump to the DB server: ssh peter@stdb01 -> password
sudo su -> to root user
systemctl status mariadb
systemctl enable mariadb
systemctl start mariadb -> error khaba then check log with journalctl -xe -u mariadb
cd /var/lib
ls -la
chown -R mysql:mysql mysql
systemctl start mariadb
systemctl status mariadb

7. Task SE LINux 
apt-get install selinux  or yum selinux* -y 
vi /etc/selinux/config
SELINUX=disabled

8. Linux remote copy:
sudo chmod 777 /tmp/nautilus.txt.gpg
sudo scp /tmp/nautilus.txt.gpg banner@stapp03:/home/webapp

9. Linux Postfix Troubleshooting:
regular ssh commands
$ ssh groot@stmail01 vai wiki
$ sudo su
$ systemctl start postfix
$ systemctl status postfix -l 
$ cat /etc/postfix/main.cf |grep inet_interface
error:
inet_interfaces = localhost
$ vi /etc/postfix/main.cf
edit the localhost parameter with # like fixing syntax error
after fix
#inet_interfaces = localhost
$ systemctl start postfix
$ systemctl status postfix
check by:
$ telnet stmail01 25



